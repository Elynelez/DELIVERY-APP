{
    "version": 2,
    "builds": [
        {
            "src": "src/**/*.js",
            "use": "@vercel/node",
            "config": {
                "commands": {
                    "beforeBuild": [
                        "npm ci"
                    ]
                }
            }
        }
    ],
    "routes": [
        {
            "source": "/(.*)",
            "dest": "/index.html"
        }
    ]
}